/**
 * Kept in step with the version in package.json. The User-Agent embeds it, so
 * the BnF can tell which build is calling from its own logs.
 */
export const PKG_VERSION = "2.1.1";
export const REPO_URL = "https://github.com/smeet666/mcp-databnf";
